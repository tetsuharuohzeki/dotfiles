# At git v2.22, this setting does not show up by `git config --global ***.***`.
# Use `git config --get **.**` or `git config core.editor` to display this value instead.
# [core]
# 	editor = nvim

[core]
	editor = nvim
